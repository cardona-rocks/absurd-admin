# Lo que no debe subirse al desplegar con la CLI (`railway up`).
# Mismo motivo que .dockerignore: el node_modules local rompe `npm ci`.
node_modules
dist
.env
.env.*
!.env.example
*.log
.DS_Store
.vite
*.tsbuildinfo
